services:
  turtlebot3-docker:
    container_name: turtlebot3-docker
    build:
      dockerfile: Dockerfile
    command: ["ros2", "launch", "turtlebot3-docker", "system-with-sim.launch.py", "slam:=true"]
    restart: unless-stopped
    privileged: true
    network_mode: host
